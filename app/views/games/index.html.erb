<h4>All Games</h4>
<ul>
  <% @games.each do |game| %>
    <strong><%= game.name%></strong></br>
    <span>City: <%= game.city %></span></br>
    <span>Location: <%= game.location %></span></br>
    <span><%= game.home_team_name %> (Home) vs. <%=  %></span></br>
  <% end %>
</ul>
